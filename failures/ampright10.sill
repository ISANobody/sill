'c <- let f : {1} = 'e <-{close 'e}; 'e <- f;
      let g : {&{a:1;b:1}<-1} = 'd <-{case 'd of 
                  | b -> close 'd
                  | a -> wait 'e;
                         abort}-< 'e;
       'd <- g -< 'e;
      'd.a;
      wait 'd;
      wait 'e;
      close 'c;;
