
'c <- let f : {1}  = 'e <-{close 'e}; 'e <- f;
      let g : {&{a:1;b:1}<-1} = 'd <-{case 'd of 
                  | a ->  wait 'e;
                          close 'd
                  | b -> close 'd}-< 'e;
       'd <- g -< 'e;
      'd.a;
      wait 'd;
      close 'c;;
